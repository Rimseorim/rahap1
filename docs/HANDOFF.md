# HANDOFF - 2026-08-29 (야간)

## 완료
- task #20 전체 종료: 8개 동작(스쿼트~수평프레스) stage-3 팁 구체 운동 인라인 백필 완료 (`111c135` 등)
- 모바일 스크롤 버그 수정 — 화이트리스트 방식이라 일부 화면에서 하단 스크롤 불가했던 것 (`5e4581b`)
- 재활 홈 화면에 의료 면책 고지 문구 추가 (`d2f817d`)
- 데드리프트 무릎 "바깥쪽" 분기 감별 갭 발견·수정: 밸거스 검사 없이 IT밴드로만 빠지던 구조 → `test-valgus-lateral` 신설, cause-b 이름/설명 일반화 + X자다리(구조적 정렬) 캐비엇 문구, 문구 길이도 축소 (`a788dbc`, `4714499`)
- PT/전문의 검수용 감별 로직 문서 생성: `docs/pt_review_전체동작.md` (8동작 전체, 1464줄) + 같은 내용을 사이드바 네비·검색 필터 있는 HTML 아티팩트로도 발행 (`57acfa7`, 아티팩트는 세션 로컬이라 링크는 세션 내에서만 유효 — 재발행 필요)
- 로그인 기능은 다른 앱으로 이관 예정, 이 앱에선 코드 안 건드리기로 확인 (메모리 저장: `project_login_deprioritized.md`)
- main 브랜치 전부 push 완료 (`57acfa7`까지)

## 진행중
없음 — 이번 세션 작업 항목은 전부 완료 상태로 종료.

## 대기
- **PT/전문의 임상 검수** — 유일한 진짜 배포 블로커. `docs/pt_review_전체동작.md` 또는 발행했던 HTML 아티팩트(세션 로컬, 재방문 시 재발행 필요) 전달해서 진행
- 실기기 전체 흐름 완주 테스트 (기존 백로그 Task #4, `docs/archive/HANDOFF-2026-07-23.md` 참조) — 미착수
- 이용약관/개인정보처리방침 문서화 (Task #3) — 로그인 이관되면 이 앱에선 불필요해질 수도, 확정 전까진 갭으로 취급
- 카카오/구글 로그인 실연동 (Task #1/#2) — 로그인 이관 예정이라 우선순위 낮음/보류 가능성 높음

## 결정사항 / 주의
- **"152개 cause 전수 클리니컬 감별 헌팅 안 함"** — 상태폭발 위험, PT 검수로 위임하기로 확정. 이번 세션에서 발견한 데드리프트/무릎 케이스 1건만 예외적으로 직접 수정.
- **"얕은 분기 패턴"** 존재 확인 (특정 부위 선택지에서만 구조적 요인이 스크리닝되는 데이터 갭) — 다른 pain_site에도 있을 가능성 높으나 수동 전수조사 안 함. `project_shallow_branch_pattern.md`에 기록, PT 검수 체크리스트 예시로 전달 예정.
- 문자열 치환 작업 시 occurrence count 확인 없이 적용하면 다른 동작의 동일 텍스트까지 잘못 바뀔 수 있음(이번 세션에 런지 cause-b 이름이 실수로 같이 바뀌었다가 즉시 발견·복구됨) — 항상 count 게이트 후 적용할 것.
- PT 검수용 아티팩트는 이 세션에서만 유효한 로컬 링크. 다음 세션에서 필요하면 `scripts`로 재추출·재생성 방법이 이번 세션 대화 기록에 있음(또는 `docs/pt_review_전체동작.md`를 그대로 다시 발행해도 됨).

## 다음 세션 권장 첫 프롬프트
`/resume`
